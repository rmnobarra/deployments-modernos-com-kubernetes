# base/analysis-template.yaml
# Template de análise que valida o canary baseado em métricas Prometheus
apiVersion: argoproj.io/v1alpha1
kind: AnalysisTemplate
metadata:
  name: success-rate
spec:
  args:
    - name: ingress-name
      # Recebe o nome do Ingress (renomeado por ambiente pelo Kustomize)
  metrics:
    - name: success-rate
      interval: 30s
      count: 5
      failureLimit: 1
      successCondition: result[0] >= 0.95
      failureCondition: result[0] < 0.90
      provider:
        prometheus:
          address: http://monitoring-kube-prometheus-prometheus.monitoring.svc.cluster.local:9090
          query: |
            sum(rate(
              nginx_ingress_controller_requests{
                ingress="{{args.ingress-name}}",
                canary!="",
                status=~"[23].."
              }[2m]
            )) /
            sum(rate(
              nginx_ingress_controller_requests{
                ingress="{{args.ingress-name}}",
                canary!=""
              }[2m]
            ))

    - name: latency-p99
      interval: 30s
      count: 5
      failureLimit: 1
      successCondition: result[0] < 0.5
      failureCondition: result[0] >= 1.0
      provider:
        prometheus:
          address: http://monitoring-kube-prometheus-prometheus.monitoring.svc.cluster.local:9090
          query: |
            histogram_quantile(0.99,
              sum(rate(
                nginx_ingress_controller_request_duration_seconds_bucket{
                  ingress="{{args.ingress-name}}",
                  canary!=""
                }[2m]
              )) by (le)
            )
